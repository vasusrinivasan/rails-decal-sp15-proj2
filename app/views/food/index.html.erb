<h1>Food Memes</h1>
<%= link_to 'Create a New Food', foods_path %>
<%= link_to 'Home', home_path %>
<% @foods.each do |food| %>
  <h3><%= link_to food.name, food %></h3>
  <img src= <%= food.imageurl %> style="width: 50%; height: 50%" />
  <p><%= food.description %></p>
<% end %>
